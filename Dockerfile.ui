# Base Docker Image
FROM python:3.11-alpine

# Disable interactive prompts during package installation
ENV DEBIAN_FRONTEND=noninteractive

# Set Working directory
WORKDIR /app

# # Install necessary packages
# RUN apt-get update && \
#     apt-get install -y --no-install-recommends \
#     cron \
#     htop && \
#     apt-get clean && \
#     rm -rf /var/lib/apt/lists/*

# Install dependencies
RUN pip install --upgrade pip \
    && pip install --no-cache-dir db-analytics-tools

# Enable access to the port
EXPOSE 8050

# Launch application
# CMD ["db_tools", "start", "--config=config.json", "--address=0.0.0.0", "--port=8050"]
CMD ["db_tools_v2", "start", "--config=config_v2.json", "--address=0.0.0.0", "--port=8050"]
